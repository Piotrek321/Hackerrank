#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    vector <int> vect;
    int N,a;
    cin >> N;
    for(int i=0;i<N;i++)
    {
        cin >> a;
        vect.push_back(a);
    }
    sort(vect.begin(),vect.end());
        for(int i=0;i<N;i++)
    {
       cout << vect[i] << " ";
    }
 
    return 0;
}

